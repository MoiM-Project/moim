<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.MemberMapper">

    <!--  admin > memberList  -->
    <select id="getMemberCount" resultType="int">
        select count(*) from member
    </select>

    <select id="getMemberList" resultType="MemberDto">
        select * from member order by idx desc
    </select>

    <select id="getMemberSearchList" parameterType="map" resultType="MemberDto">
        select * from member where nickname like concat('%'#{search}'%') or email like concat('%'#{search}'%')
    </select>

</mapper>
